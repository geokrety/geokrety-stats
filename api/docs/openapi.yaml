openapi: 3.0.3
info:
  title: GeoKrety Stats API
  version: 3.1.0
  description: "Read-only API for GeoKrety statistics,\n    resource exploration.\n\
    \nThe `/api/v3/...` contract is JSON REST based: every JSON response uses\ntop-level\
    \ `data`, `meta`, and `links`, while resource objects expose `id`,\n`type`, `attributes`,\
    \ optional `relationships`, and resource-scoped\n`links.self`.\nCollection endpoints\
    \ are cursor-first for now: clients send `limit`, follow\n`links.next`, and continue\
    \ while `meta.has_more` is true. Search endpoints\nrequire a `q` query parameter\
    \ of at least 2 characters.\nThe public API surface was intentionally reduced\
    \ during the big-bang\nmigration: redundant activity, stats, alias, and visualization\
    \ endpoints were\nremoved so the stable contract centers on core resources and\
    \ scoped\ncollections.\n\n## GeoKret Public Identifiers (GKID)\n\nGeoKrety endpoints\
    \ accept public identifiers in three forms:\n- **Numeric decimal**: `1`, `123`,\
    \ `65535` (direct GKID number)\n- **Bare hexadecimal**: `0001`, `00FF`, `FFFF`\
    \ (hex without the `GK` prefix)\n- **GK-prefixed hexadecimal**: `GK0001`, `GK00FF`,\
    \ `GKFFFF`\n  (padded hex representation)\n\nParsing is case-insensitive. Digit-only\
    \ inputs without a leading zero are\ntreated as decimal, while zero-padded digit-only\
    \ inputs are treated as\nhexadecimal.\n\nAll forms resolve to the same GeoKret.\
    \ Examples:\n- `/api/v3/geokrety/1` and `/api/v3/geokrety/GK0001` fetch the same\
    \ GeoKret\n- `/api/v3/geokrety/00FF` and `/api/v3/geokrety/GK00FF` fetch the same\
    \ GeoKret\n- `/api/v3/geokrety/255` and `/api/v3/geokrety/GK00FF` are equivalent"
servers:
- url: http://192.168.130.65:7415
  description: Local network development server
- url: http://localhost:7415
  description: Local development server
tags:
- name: System
  description: Service metadata, health checks, metrics, and websocket entrypoint
- name: Geokrety
  description: GeoKret entities and their primary relationship collections
- name: Countries
  description: Country-level aggregates and currently spotted GeoKrety
- name: Waypoints
  description: Waypoint search, details, and spotted or historical GeoKrety
- name: Users
  description: User details, activity, relationships, collections, and graphs
- name: Pictures
  description: Read-only picture metadata
paths:
  /:
    get:
      tags:
      - System
      operationId: getRootInfo
      summary: API root metadata
      description: Returns a compact self-describing payload for the running API instance.
      responses:
        '200':
          description: Root metadata payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RootInfo'
              example:
                name: GeoKrety Stats API
                version: v3
                docs: /docs
                openapi: /openapi.yaml
                health: /health
                metrics: /metrics
                root: /api/v3
            application/xml:
              schema:
                $ref: '#/components/schemas/RootInfo'
              example: <RootInfo><name>GeoKrety Stats API</name><version>v3</version><docs>/docs</docs><openapi>/openapi.yaml</openapi><health>/health</health><metrics>/metrics</metrics><root>/api/v3</root></RootInfo>
  /healtz:
    get:
      tags:
      - System
      operationId: getLiveness
      summary: Liveness check
      description: Lightweight process liveness probe without database dependency.
      responses:
        '200':
          description: Service process is alive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealtzStatus'
            application/xml:
              schema:
                $ref: '#/components/schemas/HealtzStatus'
  /health:
    get:
      tags:
      - System
      operationId: getHealth
      summary: Health check
      description: Comprehensive health check including PostgreSQL connectivity and
        active websocket client count.
      responses:
        '200':
          description: Service and database are healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
              example:
                status: ok
                activeWsClients: 0
                serverTime: '2026-03-16T20:47:17Z'
            application/xml:
              schema:
                $ref: '#/components/schemas/HealthStatus'
              example: <HealthStatus><status>ok</status><activeWsClients>0</activeWsClients><serverTime>2026-03-16T20:47:17Z</serverTime></HealthStatus>
        '503':
          $ref: '#/components/responses/ServiceUnavailableHealth'
  /metrics:
    get:
      tags:
      - System
      operationId: getPrometheusMetrics
      summary: Prometheus metrics endpoint
      description: Exposes Prometheus text exposition format metrics.
      responses:
        '200':
          description: Prometheus metrics payload
          content:
            text/plain:
              schema:
                type: string
              example: '# HELP http_requests_total Total number of HTTP requests.

                # TYPE http_requests_total counter

                http_requests_total{method="GET",path="/health",status="200"} 12

                '
  /ws:
    get:
      tags:
      - System
      operationId: openWebsocket
      summary: WebSocket upgrade endpoint
      description: Upgrades the connection to a websocket stream for live hub events.
      responses:
        '101':
          description: WebSocket protocol upgrade completed
  /openapi.yaml:
    get:
      tags:
      - System
      operationId: getOpenApiDocument
      summary: OpenAPI document
      description: Serves this OpenAPI contract as YAML when swagger support is enabled.
      responses:
        '200':
          description: OpenAPI YAML document
          content:
            application/yaml:
              schema:
                type: string
  /api/v3/geokrety:
    get:
      tags:
      - Geokrety
      operationId: listGeokrety
      summary: List GeoKrety
      parameters:
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      - $ref: '#/components/parameters/GeokretIdsParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/search:
    get:
      tags:
      - Geokrety
      operationId: searchGeokrety
      summary: Search GeoKrety
      parameters:
      - $ref: '#/components/parameters/SearchQueryParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Matching GeoKrety.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}:
    get:
      tags:
      - Geokrety
      operationId: getGeokret
      summary: GeoKret details
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      responses:
        '200':
          description: Single GeoKret resource document.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestSingleDocument'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/moves:
    get:
      tags:
      - Geokrety
      operationId: listGeokretMoves
      summary: GeoKret move history
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      - $ref: '#/components/parameters/MoveIdsParam'
      responses:
        '200':
          description: Cursor-paginated move collection for a GeoKret.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/MoveResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/moves/{moveId}:
    get:
      tags:
      - Geokrety
      operationId: getGeokretMove
      summary: GeoKret move details
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/MoveIdParam'
      responses:
        '200':
          description: Single move resource document.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestSingleDocument'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/MoveResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/loved-by:
    get:
      tags:
      - Geokrety
      operationId: listGeokretLovers
      summary: Users who love a GeoKret
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated user collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/UserResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/watched-by:
    get:
      tags:
      - Geokrety
      operationId: listGeokretWatchers
      summary: Users who watch a GeoKret
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated user collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/UserResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/pictures:
    get:
      tags:
      - Geokrety
      operationId: listGeokretPictures
      summary: Pictures attached to a GeoKret
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated picture collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/PictureResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/countries:
    get:
      tags:
      - Geokrety
      operationId: listGeokretCountries
      summary: Countries visited by a GeoKret
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated country visit collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/CountryVisitResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/geokrety/{gkid}/waypoints:
    get:
      tags:
      - Geokrety
      operationId: listGeokretWaypoints
      summary: Waypoints visited by a GeoKret
      parameters:
      - $ref: '#/components/parameters/GeokretIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated waypoint visit collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/WaypointVisitResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/countries:
    get:
      tags:
      - Countries
      operationId: listCountries
      summary: List countries
      parameters:
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      - $ref: '#/components/parameters/CountryIdsParam'
      responses:
        '200':
          description: Cursor-paginated country collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/CountryResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/countries/{code}:
    get:
      tags:
      - Countries
      operationId: getCountry
      summary: Country details
      parameters:
      - $ref: '#/components/parameters/CountryCodeParam'
      responses:
        '200':
          description: Single country resource document.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestSingleDocument'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/CountryResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/countries/{code}/geokrety:
    get:
      tags:
      - Countries
      operationId: listCountryGeokrety
      summary: GeoKrety currently in a country
      parameters:
      - $ref: '#/components/parameters/CountryCodeParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection scoped to a country.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/waypoints/search:
    get:
      tags:
      - Waypoints
      operationId: searchWaypoints
      summary: Search waypoints
      parameters:
      - $ref: '#/components/parameters/SearchQueryParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Matching waypoint collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/WaypointResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/waypoints/{code}:
    get:
      tags:
      - Waypoints
      operationId: getWaypoint
      summary: Waypoint details
      parameters:
      - $ref: '#/components/parameters/WaypointCodeParam'
      responses:
        '200':
          description: Single waypoint resource document.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestSingleDocument'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/WaypointResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/waypoints/{code}/geokrety-current:
    get:
      tags:
      - Waypoints
      operationId: listWaypointCurrentGeokrety
      summary: GeoKrety currently at a waypoint
      parameters:
      - $ref: '#/components/parameters/WaypointCodeParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/waypoints/{code}/geokrety-past:
    get:
      tags:
      - Waypoints
      operationId: listWaypointPastGeokrety
      summary: GeoKrety previously seen at a waypoint
      parameters:
      - $ref: '#/components/parameters/WaypointCodeParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated historical GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users:
    get:
      tags:
      - Users
      operationId: listUsers
      summary: List users
      parameters:
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      - $ref: '#/components/parameters/UserIdsParam'
      responses:
        '200':
          description: Cursor-paginated user collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/UserResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/search:
    get:
      tags:
      - Users
      operationId: searchUsers
      summary: Search users
      parameters:
      - $ref: '#/components/parameters/SearchQueryParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Matching user collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/UserResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}:
    get:
      tags:
      - Users
      operationId: getUser
      summary: User details
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      responses:
        '200':
          description: Single user resource document.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestSingleDocument'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/UserResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/geokrety-owned:
    get:
      tags:
      - Users
      operationId: listUserOwnedGeokrety
      summary: GeoKrety owned by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/geokrety-found:
    get:
      tags:
      - Users
      operationId: listUserFoundGeokrety
      summary: GeoKrety found by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/geokrety-loved:
    get:
      tags:
      - Users
      operationId: listUserLovedGeokrety
      summary: GeoKrety loved by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/geokrety-watched:
    get:
      tags:
      - Users
      operationId: listUserWatchedGeokrety
      summary: GeoKrety watched by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated GeoKrety collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/GeokretResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/pictures:
    get:
      tags:
      - Users
      operationId: listUserPictures
      summary: Pictures authored or owned by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated picture collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/PictureResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/countries:
    get:
      tags:
      - Users
      operationId: listUserCountries
      summary: Countries visited by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated country visit collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/CountryVisitResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/users/{id}/waypoints:
    get:
      tags:
      - Users
      operationId: listUserWaypoints
      summary: Waypoints visited by a user
      parameters:
      - $ref: '#/components/parameters/UserIdParam'
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      responses:
        '200':
          description: Cursor-paginated waypoint visit collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/WaypointVisitResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/pictures:
    get:
      tags:
      - Pictures
      operationId: listPictures
      summary: List pictures
      parameters:
      - $ref: '#/components/parameters/LimitParam'
      - $ref: '#/components/parameters/CursorParam'
      - $ref: '#/components/parameters/PictureIdsParam'
      responses:
        '200':
          description: Cursor-paginated picture collection.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestCollectionDocument'
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: '#/components/schemas/PictureResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v3/pictures/{id}:
    get:
      tags:
      - Pictures
      operationId: getPicture
      summary: Picture details
      parameters:
      - $ref: '#/components/parameters/PictureIdParam'
      responses:
        '200':
          description: Single picture resource document.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/JsonRestSingleDocument'
                - type: object
                  properties:
                    data:
                      $ref: '#/components/schemas/PictureResource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
components:
  parameters:
    LimitParam:
      in: query
      name: limit
      description: Maximum number of items to return. Cursor-paginated endpoints default
        to 20 and reject values above the endpoint-specific maximum.
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 1000
        default: 20
    CursorParam:
      in: query
      name: cursor
      description: Opaque cursor token returned in `links.next`. Do not construct
        or modify this value on the client.
      required: false
      schema:
        type: string
    OffsetParam:
      in: query
      name: offset
      deprecated: true
      description: Zero-based starting offset for paginated responses. Default 0.
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
    SearchQueryParam:
      in: query
      name: q
      description: Search text. Must be at least 2 characters long.
      required: true
      schema:
        type: string
        minLength: 2
      example: gk
    GeokretIdsParam:
      in: query
      name: ids
      description: Optional comma-separated list of public GeoKret identifiers. When
        provided, the endpoint returns exactly the requested resources and ignores
        cursor pagination.
      required: false
      schema:
        type: string
        pattern: ^(?:\s*(?:[Gg][Kk])?0*[1-9A-Fa-f][0-9A-Fa-f]*\s*)(?:,\s*(?:[Gg][Kk])?0*[1-9A-Fa-f][0-9A-Fa-f]*\s*)*$
      example: GK0001,GK00FF
    MoveIdsParam:
      in: query
      name: ids
      description: Optional comma-separated list of numeric move identifiers. When
        provided, the endpoint returns exactly the requested move resources and ignores
        cursor pagination.
      required: false
      schema:
        type: string
        pattern: ^\s*[1-9][0-9]*(?:\s*,\s*[1-9][0-9]*)*\s*$
      example: 10,11,15
    CountryIdsParam:
      in: query
      name: ids
      description: Optional comma-separated list of ISO-3166 alpha-2 country codes.
        When provided, the endpoint returns exactly the requested country resources
        and ignores cursor pagination.
      required: false
      schema:
        type: string
        pattern: ^\s*[A-Za-z]{2}(?:\s*,\s*[A-Za-z]{2})*\s*$
      example: PL,DE,FR
    UserIdsParam:
      in: query
      name: ids
      description: Optional comma-separated list of numeric user identifiers. When
        provided, the endpoint returns exactly the requested user resources and ignores
        cursor pagination.
      required: false
      schema:
        type: string
        pattern: ^\s*[1-9][0-9]*(?:\s*,\s*[1-9][0-9]*)*\s*$
      example: 1,42,99
    PictureIdsParam:
      in: query
      name: ids
      description: Optional comma-separated list of numeric picture identifiers. When
        provided, the endpoint returns exactly the requested picture resources and
        ignores cursor pagination.
      required: false
      schema:
        type: string
        pattern: ^\s*[1-9][0-9]*(?:\s*,\s*[1-9][0-9]*)*\s*$
      example: 5,6,7
    GeokretIdParam:
      in: path
      name: gkid
      description: 'GeoKret public identifier. Accepts numeric decimal (e.g., 1),
        bare

        hexadecimal (e.g., 00FF), or GK-prefixed hexadecimal format (e.g.,

        GK0001, GKFFFF).

        Parsing is case-insensitive. Bare digit-only values without a leading zero

        are treated as decimal, while zero-padded digit-only values are treated as

        hexadecimal.

        '
      required: true
      schema:
        type: string
        pattern: ^(?:[Gg][Kk])?0*[1-9A-Fa-f][0-9A-Fa-f]*$
      examples:
        numeric:
          value: '1'
          description: Numeric decimal form
        bareHex:
          value: 00FF
          description: Hexadecimal form without the GK prefix
        hexPrefixed:
          value: GK0001
          description: GK-prefixed hexadecimal form
        largeHex:
          value: GKFFFF
          description: Maximum 16-bit hex value
    MoveIdParam:
      in: path
      name: moveId
      description: Internal numeric move identifier.
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    UserIdParam:
      in: path
      name: id
      description: Internal numeric user identifier.
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    CountryCodeParam:
      in: path
      name: code
      description: Uppercase ISO-3166 alpha-2 country code.
      required: true
      schema:
        type: string
        minLength: 2
        maxLength: 2
        pattern: ^[A-Z]{2}$
      example: PL
    WaypointCodeParam:
      in: path
      name: code
      description: Waypoint code or source-specific cache identifier.
      required: true
      schema:
        type: string
        minLength: 2
        maxLength: 32
        pattern: ^[A-Za-z0-9_-]{2,32}$
      example: GC12345
    PictureIdParam:
      in: path
      name: id
      description: Internal numeric picture identifier.
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
  responses:
    BadRequest:
      description: Request validation failed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/JsonRestErrorDocument'
          example:
            error:
              code: BAD_REQUEST
              message: invalid identifier
            timestamp: '2026-03-17T09:12:34Z'
        application/xml:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: invalid identifier
            timestamp: '2026-03-17T09:12:34Z'
    NotFound:
      description: The requested resource does not exist.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/JsonRestErrorDocument'
          example:
            error:
              code: NOT_FOUND
              message: resource not found
            timestamp: '2026-03-17T09:12:34Z'
        application/xml:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: resource not found
            timestamp: '2026-03-17T09:12:34Z'
    InternalServerError:
      description: The server failed to read or prepare the requested data.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/JsonRestErrorDocument'
          example:
            error:
              code: INTERNAL_ERROR
              message: failed to fetch resource
            timestamp: '2026-03-17T09:12:34Z'
        application/xml:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: failed to fetch resource
            timestamp: '2026-03-17T09:12:34Z'
    ServiceUnavailableHealth:
      description: Health check degraded because the database is unavailable.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/HealthDegradedStatus'
          example:
            status: degraded
            error: database_unavailable
        application/xml:
          schema:
            $ref: '#/components/schemas/HealthDegradedStatus'
          example:
            status: degraded
            error: database_unavailable
  schemas:
    JsonRestResourceLinks:
      type: object
      additionalProperties: false
      properties:
        self:
          type: string
    JsonRestDocumentLinks:
      type: object
      additionalProperties: false
      properties:
        self:
          type: string
        next:
          type: string
    JsonRestIdentifier:
      type: object
      required:
      - type
      - id
      properties:
        type:
          type: string
        id:
          type: string
    JsonRestRelationship:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/JsonRestIdentifier'
        links:
          $ref: '#/components/schemas/JsonRestResourceLinks'
    JsonRestMeta:
      type: object
      required:
      - execution_time_ms
      properties:
        execution_time_ms:
          type: integer
          description: Milliseconds spent handling the request.
        limit:
          type: integer
          description: Requested collection size for cursor collections.
        has_more:
          type: boolean
          description: Indicates whether `links.next` can be followed.
        page:
          type: integer
        per_page:
          type: integer
        total_items:
          type: integer
        total_pages:
          type: integer
    JsonRestErrorPayload:
      type: object
      required:
      - code
      - message
      properties:
        code:
          type: string
        message:
          type: string
    JsonRestErrorDocument:
      type: object
      required:
      - error
      - timestamp
      properties:
        error:
          $ref: '#/components/schemas/JsonRestErrorPayload'
        timestamp:
          type: string
          format: date-time
    JsonRestResource:
      type: object
      required:
      - id
      - type
      properties:
        id:
          type: string
        type:
          type: string
        attributes:
          type: object
          additionalProperties: true
        relationships:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/JsonRestRelationship'
        links:
          $ref: '#/components/schemas/JsonRestResourceLinks'
    JsonRestSingleDocument:
      type: object
      required:
      - data
      - meta
      - links
      properties:
        data:
          $ref: '#/components/schemas/JsonRestResource'
        meta:
          $ref: '#/components/schemas/JsonRestMeta'
        links:
          $ref: '#/components/schemas/JsonRestDocumentLinks'
    JsonRestCollectionDocument:
      type: object
      required:
      - data
      - meta
      - links
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/JsonRestResource'
        meta:
          $ref: '#/components/schemas/JsonRestMeta'
        links:
          $ref: '#/components/schemas/JsonRestDocumentLinks'
    GeokretResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - geokret
    MoveResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - move
    UserResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - user
    PictureResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - picture
    CountryResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - country
    WaypointResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - waypoint
    CountryVisitResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - country_visit
    WaypointVisitResource:
      allOf:
      - $ref: '#/components/schemas/JsonRestResource'
      - type: object
        properties:
          type:
            type: string
            enum:
            - waypoint_visit
    RootInfo:
      type: object
      xml:
        name: RootInfo
      required:
      - name
      - version
      - docs
      - openapi
      - health
      - metrics
      - root
      properties:
        name:
          type: string
        version:
          type: string
        docs:
          type: string
        openapi:
          type: string
        health:
          type: string
        metrics:
          type: string
        root:
          type: string
    HealtzStatus:
      type: object
      xml:
        name: HealtzStatus
      required:
      - status
      - serverTime
      properties:
        status:
          type: string
          enum:
          - ok
        serverTime:
          type: string
          format: date-time
      example:
        status: ok
        serverTime: '2026-03-17T09:12:34Z'
    HealthStatus:
      type: object
      xml:
        name: HealthStatus
      required:
      - status
      - activeWsClients
      - serverTime
      properties:
        status:
          type: string
          enum:
          - ok
        activeWsClients:
          type: integer
        serverTime:
          type: string
          format: date-time
    HealthDegradedStatus:
      type: object
      xml:
        name: HealthStatus
      required:
      - status
      - error
      properties:
        status:
          type: string
          enum:
          - degraded
        error:
          type: string
          enum:
          - database_unavailable
    ErrorResponse:
      type: object
      xml:
        name: ErrorResponse
      required:
      - error
      - timestamp
      properties:
        error:
          type: string
          description: Human-readable error message
        timestamp:
          type: string
          format: date-time
          description: Timestamp when the error occurred
    PaginationMeta:
      type: object
      xml:
        name: PaginationMeta
      required:
      - limit
      - offset
      - count
      properties:
        limit:
          type: integer
          description: Requested page size
        offset:
          type: integer
          description: Requested start offset
        count:
          type: integer
          description: Actual count of items returned
    ResponseMeta:
      type: object
      xml:
        name: ResponseMeta
      required:
      - requestedAt
      - queryMs
      - pagination
      properties:
        requestedAt:
          type: string
          format: date-time
          description: Server-side timestamp of request handling
        queryMs:
          type: integer
          description: Milliseconds spent executing the database query
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
    EnvelopeBase:
      type: object
      xml:
        name: Envelope
      required:
      - data
      - meta
      properties:
        data:
          description: Response payload (type depends on endpoint)
        meta:
          $ref: '#/components/schemas/ResponseMeta'
    GeoJSONPoint:
      type: object
      required:
      - type
      - coordinates
      properties:
        type:
          type: string
          enum:
          - Point
        coordinates:
          type: array
          minItems: 2
          maxItems: 2
          items:
            type: number
          example:
          - 10.04402
          - 53.59728
    GeoJSONFeature:
      type: object
      required:
      - type
      - geometry
      - properties
      properties:
        type:
          type: string
          enum:
          - Feature
        geometry:
          $ref: '#/components/schemas/GeoJSONPoint'
        properties:
          type: object
    GeoJSONFeatureCollection:
      type: object
      required:
      - type
      - features
      properties:
        type:
          type: string
          enum:
          - FeatureCollection
        features:
          type: array
          items:
            $ref: '#/components/schemas/GeoJSONFeature'
    PictureTypeStats:
      type: object
      properties:
        geokretAvatars:
          type: integer
        geokretMoves:
          type: integer
        userAvatars:
          type: integer
    GeokretyTypeStats:
      type: object
      properties:
        traditional:
          type: integer
        book:
          type: integer
        human:
          type: integer
        coin:
          type: integer
        kretypost:
          type: integer
        pebble:
          type: integer
        car:
          type: integer
        playingcard:
          type: integer
        dogtag:
          type: integer
        jigsaw:
          type: integer
        easteregg:
          type: integer
    MoveTypeStats:
      type: object
      properties:
        dropped:
          type: integer
        grabbed:
          type: integer
        commented:
          type: integer
        seen:
          type: integer
        archived:
          type: integer
        dipped:
          type: integer
    GlobalStats:
      type: object
      properties:
        totalGeokrety:
          type: integer
          description: Total number of active GeoKrety
        totalGeokretyHidden:
          type: integer
          description: Total hidden GeoKrety
        totalMoves:
          type: integer
          description: Total moves across all GeoKrety
        movesLast30Days:
          type: integer
          description: Moves recorded in the last 30 days
        registeredUsers:
          type: integer
          description: Registered user accounts
        activeUsers:
          type: integer
          description: Users active right now
        activeUsersLast30d:
          type: integer
          description: Distinct users active in last 30 days
        countriesReached:
          type: integer
          description: Countries with at least one GeoKret record
        picturesUploaded:
          type: integer
          description: Total pictures in database
        picturesByType:
          $ref: '#/components/schemas/PictureTypeStats'
        geokretyByType:
          $ref: '#/components/schemas/GeokretyTypeStats'
        movesByType:
          $ref: '#/components/schemas/MoveTypeStats'
    RecentMove:
      type: object
      properties:
        id:
          type: integer
          format: int64
        geokretName:
          type: string
        type:
          type: string
        username:
          type: string
        country:
          type: string
        countryFlag:
          type: string
        timestamp:
          type: string
          format: date-time
    LeaderboardUser:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        movesCount:
          type: integer
        rank:
          type: integer
    CountryStats:
      type: object
      properties:
        code:
          type: string
          maxLength: 2
        name:
          type: string
        geokretyCount:
          type: integer
        visible:
          type: boolean
    RecentBorn:
      type: object
      properties:
        id:
          type: integer
          format: int64
        gkid:
          type: string
          example: GK0001
        name:
          type: string
        type:
          type: integer
        typeName:
          type: string
        ownerId:
          type: integer
          format: int64
          nullable: true
        ownerName:
          type: string
        bornAt:
          type: string
          format: date-time
    RecentLoved:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        gkid:
          type: string
          nullable: true
          example: GK0001
        geokretName:
          type: string
        userId:
          type: integer
          format: int64
        username:
          type: string
        lovedAt:
          type: string
          format: date-time
    RecentWatched:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        gkid:
          type: string
          nullable: true
          example: GK0001
        geokretName:
          type: string
        userId:
          type: integer
          format: int64
        username:
          type: string
        watchedAt:
          type: string
          format: date-time
    ActiveCountry:
      type: object
      properties:
        code:
          type: string
        moves:
          type: integer
        uniqueUsers:
          type: integer
        lastActivityAt:
          type: string
          format: date-time
        flag:
          type: string
    ActiveWaypoint:
      type: object
      properties:
        waypoint:
          type: string
        country:
          type: string
          nullable: true
        moves:
          type: integer
        lastActivityAt:
          type: string
          format: date-time
        lat:
          type: number
          format: double
          nullable: true
        lon:
          type: number
          format: double
          nullable: true
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
          nullable: true
    RecentRegisteredUser:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        joinedAt:
          type: string
          format: date-time
        homeCountry:
          type: string
          nullable: true
    RecentActiveUser:
      type: object
      properties:
        userId:
          type: integer
          format: int64
        username:
          type: string
        movesCount:
          type: integer
        countriesCount:
          type: integer
        lastMoveAt:
          type: string
          format: date-time
    HourlyHeatmapCell:
      type: object
      properties:
        activityDate:
          type: string
          format: date
        hourUtc:
          type: integer
          description: Hour number (0-23 UTC)
        moveType:
          type: integer
        moveCount:
          type: integer
          description: Number of moves in this cell
    CountryFlow:
      type: object
      properties:
        yearMonth:
          type: string
          format: date
        fromCountry:
          type: string
        toCountry:
          type: string
        moveCount:
          type: integer
          description: Number of GeoKret transitions between countries
        uniqueGkCount:
          type: integer
        fromFlag:
          type: string
        toFlag:
          type: string
    TopCache:
      type: object
      properties:
        waypointCode:
          type: string
        totalGkVisits:
          type: integer
        distinctGks:
          type: integer
    FirstFinderLeaderboardEntry:
      type: object
      properties:
        userId:
          type: integer
          format: int64
        username:
          type: string
        firstFinds:
          type: integer
        rank:
          type: integer
    DistanceRecord:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        gkid:
          type: string
          nullable: true
          example: GK0001
        geokretName:
          type: string
        kmTotal:
          type: number
          format: double
        rank:
          type: integer
    DormancyRecord:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        gkid:
          type: string
          nullable: true
          example: GK0001
        geokretName:
          type: string
        lastTouch:
          type: string
          format: date-time
          nullable: true
        dormancySeconds:
          type: integer
        dormancyDays:
          type: number
    MultiplierVelocityRecord:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        gkid:
          type: string
          nullable: true
          example: GK0001
        geokretName:
          type: string
        lastChange:
          type: string
          format: date-time
          nullable: true
        avgDelta:
          type: number
          format: double
    GeokretListItem:
      type: object
      xml:
        name: GeokretListItem
      properties:
        id:
          type: integer
          format: int64
          description: Internal database identifier (for API compatibility)
        gkid:
          type: string
          example: GK0001
          description: Public GeoKret identifier
        name:
          type: string
        type:
          type: integer
          description: GeoKret type code
        typeName:
          type: string
        missing:
          type: boolean
        missingAt:
          type: string
          format: date-time
          nullable: true
        ownerId:
          type: integer
          format: int64
          nullable: true
        ownerUsername:
          type: string
          nullable: true
        holderId:
          type: integer
          format: int64
          nullable: true
        holderUsername:
          type: string
          nullable: true
        lat:
          type: number
          format: double
          nullable: true
        lon:
          type: number
          format: double
          nullable: true
        country:
          type: string
          maxLength: 2
          nullable: true
        waypoint:
          type: string
          nullable: true
        lovesCount:
          type: integer
        picturesCount:
          type: integer
        bornAt:
          type: string
          format: date-time
          nullable: true
        lastMoveAt:
          type: string
          format: date-time
          nullable: true
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
          nullable: true
    GeokretDetails:
      type: object
      xml:
        name: GeokretDetails
      properties:
        id:
          type: integer
          format: int64
          description: Internal database identifier
        gkid:
          type: string
          example: GK0001
          description: Public GeoKret identifier
        name:
          type: string
        type:
          type: integer
        typeName:
          type: string
        missingAt:
          type: string
          format: date-time
          nullable: true
        missing:
          type: boolean
        ownerId:
          type: integer
          format: int64
          nullable: true
        ownerUsername:
          type: string
          nullable: true
        holderId:
          type: integer
          format: int64
          nullable: true
        holderUsername:
          type: string
          nullable: true
        country:
          type: string
          maxLength: 2
          nullable: true
        waypoint:
          type: string
          nullable: true
        lat:
          type: number
          format: double
          nullable: true
        lon:
          type: number
          format: double
          nullable: true
        lovesCount:
          type: integer
        picturesCount:
          type: integer
        bornAt:
          type: string
          format: date-time
        lastMoveAt:
          type: string
          format: date-time
          nullable: true
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
          nullable: true
        mission:
          type: string
          nullable: true
        distanceKm:
          type: number
          format: double
        cachesCount:
          type: integer
        commentsHidden:
          type: boolean
    MoveRecord:
      type: object
      xml:
        name: MoveRecord
      properties:
        id:
          type: integer
          format: int64
        geokretId:
          type: integer
          format: int64
        moveType:
          type: integer
        moveTypeName:
          type: string
        authorId:
          type: integer
          format: int64
          nullable: true
        username:
          type: string
          nullable: true
        movedOn:
          type: string
          format: date-time
        createdOn:
          type: string
          format: date-time
        country:
          type: string
          maxLength: 2
          nullable: true
        waypoint:
          type: string
          nullable: true
        elevation:
          type: integer
          format: int64
          nullable: true
        kmDistance:
          type: number
          format: double
          nullable: true
        picturesCount:
          type: integer
        commentsCount:
          type: integer
        comment:
          type: string
          nullable: true
        commentHidden:
          type: boolean
        lat:
          type: number
          format: double
          nullable: true
        lon:
          type: number
          format: double
          nullable: true
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
          nullable: true
    SocialUserEntry:
      type: object
      properties:
        userId:
          type: integer
          format: int64
        username:
          type: string
        at:
          type: string
          format: date-time
    UserNetworkEdge:
      type: object
      properties:
        userId:
          type: integer
          format: int64
        relatedUserId:
          type: integer
          format: int64
        relatedUsername:
          type: string
        sharedGeokretyCount:
          type: integer
        firstSeenAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
    GeokretTimelineEvent:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        eventType:
          type: string
        occurredAt:
          type: string
          format: date-time
        actorUserId:
          type: integer
          format: int64
          nullable: true
        actorUsername:
          type: string
          nullable: true
    GeokretCirculation:
      type: object
      properties:
        geokretId:
          type: integer
          format: int64
        gkid:
          type: string
          nullable: true
          example: GK0001
        geokretName:
          type: string
        users:
          type: integer
        interactions:
          type: integer
        avgInteractionsPerUser:
          type: number
          format: double
    PictureInfo:
      type: object
      properties:
        id:
          type: integer
          format: int64
        type:
          type: integer
        filename:
          type: string
          nullable: true
        caption:
          type: string
          nullable: true
        key:
          type: string
          nullable: true
        geokretId:
          type: integer
          format: int64
          nullable: true
        moveId:
          type: integer
          format: int64
          nullable: true
        userId:
          type: integer
          format: int64
          nullable: true
        authorId:
          type: integer
          format: int64
          nullable: true
        authorUsername:
          type: string
          nullable: true
        uploadedOn:
          type: string
          format: date-time
          nullable: true
        createdOn:
          type: string
          format: date-time
    GeokretCountryVisit:
      type: object
      properties:
        countryCode:
          type: string
          maxLength: 2
        firstVisitedAt:
          type: string
          format: date-time
        moveCount:
          type: integer
        flag:
          type: string
    GeokretWaypointVisit:
      type: object
      properties:
        waypointCode:
          type: string
        country:
          type: string
          maxLength: 2
          nullable: true
        visitCount:
          type: integer
        firstVisitedAt:
          type: string
          format: date-time
        lastVisitedAt:
          type: string
          format: date-time
        lat:
          type: number
          format: double
          nullable: true
        lon:
          type: number
          format: double
          nullable: true
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
          nullable: true
    CountryCount:
      type: object
      properties:
        countryCode:
          type: string
          maxLength: 2
        value:
          type: integer
        firstAt:
          type: string
          format: date-time
          nullable: true
        flag:
          type: string
    ElevationPoint:
      type: object
      properties:
        moveId:
          type: integer
          format: int64
        movedOn:
          type: string
          format: date-time
        elevation:
          type: integer
          description: Elevation in meters
        kmDistance:
          type: number
          format: double
          nullable: true
        country:
          type: string
          nullable: true
        waypoint:
          type: string
          nullable: true
    DayHeatmapCell:
      type: object
      properties:
        day:
          type: string
          format: date
        moveCount:
          type: integer
    HourHeatmapCell:
      type: object
      properties:
        dayOfWeek:
          type: integer
          description: Day of week (0-6)
        hourUtc:
          type: integer
          description: Hour of day (0-23 UTC)
        moveCount:
          type: integer
    CountryDetails:
      type: object
      properties:
        code:
          type: string
          maxLength: 2
        name:
          type: string
        geokretyCount:
          type: integer
        visible:
          type: boolean
        moves:
          type: integer
          description: Total moves in country
        lastMoveAt:
          type: string
          format: date-time
          nullable: true
    WaypointSummary:
      type: object
      properties:
        id:
          type: integer
          format: int64
        waypointCode:
          type: string
        source:
          type: string
          description: Cache source (GC, UK, etc.)
        country:
          type: string
          maxLength: 2
        lat:
          type: number
          format: double
        lon:
          type: number
          format: double
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
    WaypointDetails:
      type: object
      properties:
        id:
          type: integer
          format: int64
        waypointCode:
          type: string
        source:
          type: string
        country:
          type: string
          maxLength: 2
        name:
          type: string
          nullable: true
        lat:
          type: number
          format: double
        lon:
          type: number
          format: double
        geojson:
          $ref: '#/components/schemas/GeoJSONPoint'
        currentGeokretyCount:
          type: integer
        totalVisits:
          type: integer
    UserDetails:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        joinedAt:
          type: string
          format: date-time
        homeCountry:
          type: string
          maxLength: 2
          nullable: true
        avatarId:
          type: integer
          format: int64
          nullable: true
        picturesCount:
          type: integer
        ownedGeokretyCount:
          type: integer
        movesCount:
          type: integer
        distinctGeokretyCount:
          type: integer
          description: Number of unique GeoKrety touched
        activeCountriesCount:
          type: integer
        lastMoveAt:
          type: string
          format: date-time
          nullable: true
        homeCountryFlag:
          type: string
          nullable: true
    UserCountryVisit:
      type: object
      properties:
        country:
          type: string
          maxLength: 2
        visitDate:
          type: string
          format: date
        moveCount:
          type: integer
    UserWaypointVisit:
      type: object
      properties:
        waypointCode:
          type: string
        country:
          type: string
          maxLength: 2
        visitDate:
          type: string
          format: date
    UserContinentCoverage:
      type: object
      properties:
        continent:
          type: string
        countries:
          type: integer
    UserSearchResult:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        joinedAt:
          type: string
          format: date-time
        movesCount:
          type: integer
        lastMoveAt:
          type: string
          format: date-time
          nullable: true
